# dependencies
node_modules/
/.pnp
.pnp.js

# testing
/coverage

# production - allow build folder on gh-pages branch for deployment
# /build

# misc
.DS_Store
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Large files that shouldn't be in repo
oracleJdk-25/
ios-safari-remote-debug-kit/

# Root-level build artifacts (copies from build/ folder for GitHub Pages)
# Note: These ARE needed on gh-pages branch for deployment, so commenting out
# /asset-manifest.json
# /index.html
# /service-worker.js
# /static/
# /favicon.svg
